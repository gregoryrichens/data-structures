function shortestRoute(network, sender, recipient) {
  // write a dynamic programming function
  // initiate a sender variable
  // initiate a recipient variable
  // initiate an array to store path

  // recursive helper function for finding route
}